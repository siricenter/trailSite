<div id="search">    
    <%= form_tag("/search", method: "get") do %>
        <p>
            <%= label_tag(:basic_search, "Search for:") %>
            <%= text_field_tag(:basic_search, params[:basic_search]) %>
            <%= submit_tag("Search", name: nil) %>
        </p>
    <% end %>
</div>