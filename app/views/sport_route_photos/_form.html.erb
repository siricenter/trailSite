<%= form_for(@sport_route_photo) do |f| %>
  <% if @sport_route_photo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sport_route_photo.errors.count, "error") %> prohibited this sport_route_photo from being saved:</h2>

      <ul>
      <% @sport_route_photo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <div class="form-field <%=if @sport_route_photo.errors[:title].present? then 'has-error' else 'has-success' end %>">
      <%= f.label :title %><br>
      <%= f.text_field :title, class: "form-control" %>
    </div>

    <div class="form-field <%=if @sport_route_photo.errors[:sport_route_id].present? then 'has-error' else 'has-success' end %>">
      <%= f.label :sport_route_id %><br>
      <%= select("sport_route_photo", "sport_route_id", SportRoute.all.collect {|r| [ r.name, r.id ] }, {include_blank: ''},
       class: "form-control" )  %>
    </div>

    <div class="form-field <%=if @sport_route_photo.errors[:url].present? then 'has-error' else 'has-success' end %>">
      <%= f.label :url %><br>
      <%= f.text_field :url, class: "form-control" %>
    </div>
    <div class="actions col-md-12">
      <%= f.submit class: "form-button" %>
    </div>
  </div>
<% end %>
