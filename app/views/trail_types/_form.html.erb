<%= form_for(@trail_type) do |f| %>
  <% if @trail_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@trail_type.errors.count, "error") %> prohibited this trail_type from being saved:</h2>

      <ul>
      <% @trail_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
